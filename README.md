# 4Pilots Landing Page

Static HTML + CSS landing page for the 4Pilots mobile app.

## Deploy
- Upload `index.html`, `styles.css`, and the `assets/` folder to any static host.
- Ensure the site root preserves the same paths (e.g., `/assets/screenshots/...`).

## Local preview
Open `index.html` in a browser or run a simple static server.
